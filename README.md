# WithNuri

WithNuri is an open source desktop companion app for showing a live, background-removed pet overlay from a phone or home camera stream.

The project is currently building the first stream setup layer. See `docs/withnuri-design.md` for the design.
